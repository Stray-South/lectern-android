<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <!-- D2D transfer exclusions: calibration weights must not transfer to a new device -->
    <device-transfer>
        <exclude domain="dataDir" path="files/datastore/calibration_prefs.preferences_pb" />
    </device-transfer>
</data-extraction-rules>
